# Cryptocurrency API Configuration
# This file contains default settings that can be overridden by environment variables

# Binance API Configuration
BINANCE_INTERVAL=2          # Interval in seconds (default: 2)
BINANCE_PORT=10000          # Server port (default: 10000)

# CoinGecko API Configuration  
COINGECKO_INTERVAL=5        # Interval in seconds (default: 5)
COINGECKO_PORT=10001        # Server port (default: 10001)

# Rate Limit Information
# Binance: 1200 calls/minute (safe minimum: 0.05s, recommended: 2s+)
# CoinGecko: 50 calls/minute (safe minimum: 1.2s, recommended: 5s+)

# Usage Examples:
# 1. Source this file: source .env
# 2. Override in command: BINANCE_INTERVAL=1 npm start
# 3. Use npm scripts: npm run start:1s
# 4. Use config script: ./config-intervals.sh quick 3